const initialState = {
}
export function appReducer(state = initialState, action) {
    var newState = state;
    switch (action.type) {
        // case 'SET_BOARD':
            // newState = { board:action.board }
            // break;
        default:
    }

    return newState;

}